# Django Configuration
DEBUG=1
SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_SETTINGS_MODULE=ab_reservas_project.settings

# Database Configuration
# Set to 'postgresql' to use PostgreSQL, or 'sqlite' for SQLite
DATABASE=postgresql

# PostgreSQL Configuration (only needed if DATABASE=postgresql)
POSTGRES_DB=ab_reservas
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432

# SQLite Configuration (only needed if DATABASE=sqlite)
# SQLITE_DB_PATH=db.sqlite3

# Allowed Hosts
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
